# VideoRec 开发方案：4 路多摄像头 + 外部设备 TTL 硬同步

> 版本：v1.0（2026-08-03）
> 状态：方案设计（硬件待采购，软件 M1-M3 待排期）
> 关联代码：`D:\video_rec`（main.py → ui/main_window.py, core/*）

---

## 1. 背景与目标

当前 VideoRec 为单摄像头录制 + 参考帧一致性控制。目标升级为：

1. **4 路摄像头同时录制 4 只小鼠**（或 4 视角），画质清晰、帧率稳定、同步启停
2. **与光遗传 / fiberphotometry 设备硬同步**：利用设备 TTL trigger in/out 实现帧级时间对齐
3. 保持现有能力：触发区点击联动、参考帧/虚影、ORB 对齐、光照判定、录制备注

**同步需求分级：**

| 同步对象 | 精度需求 | 手段 |
|---|---|---|
| 视频 ↔ 视频（4 路） | 帧级（±1 帧 @30fps 可接受） | 软件同步启停 + 每帧时间戳；进阶用硬件触发 |
| 视频 ↔ fiberphotometry | 帧级（光纤采样通常 1kHz） | **TTL 帧同步脉冲**（30Hz）写入光纤时间轴 |
| 视频 ↔ 光遗传刺激 | 事件级（刺激起止时间） | **TTL 事件记录**（刺激 out → 视频侧记录帧号） |

---

## 2. 多摄像头硬件方案

### 2.1 决策前提

- **同步等级决定成本**：行为学事件（嗅探/爬跨/攻击）为秒级，软件同步够用；帧级精确同步需硬件触发相机
- **协议红线**：只选**标准 UVC 免驱**相机（OpenCV `cv2.VideoCapture` 直开）。
  ⚠️ 海康/大华/Basler/FLIR 机器视觉相机多为 USB3 Vision 协议（需 SDK），VideoRec 不兼容，下单前必须确认

### 2.2 方案 A：预算全局快门（推荐起步，~¥500-800/路）

| 部件 | 型号/规格 | 参考价 | 说明 |
|---|---|---|---|
| 相机 | OV9281 全局快门 UVC 模块（720p@120fps） | ¥80-200 | 全局快门无果冻畸变（小鼠快速移动关键）；选 CS 接口可换镜头版 |
| 相机升级 | OV2311 全局快门 UVC（1600×1300） | ¥150-300 | 2MP，清晰度更高 |
| 镜头 | CS 接口定焦 4-8mm，f1.4 大光圈 | ¥30-60 | 避免广角（畸变影响位置分析） |
| 红外版 | 无 IR-cut + 850nm 补光灯板 | ¥20-50 | 暗环境/夜间行为阶段 |
| 同步 | 软件同步（VideoRec 时间戳） | ¥0 | 现有功能，零成本 |

### 2.3 方案 B：工业 UVC + 硬件触发（帧级同步，~¥2500-5000/路）

| 品牌 | 型号 | 要点 |
|---|---|---|
| 映美精 (The Imaging Source) | DMK 33UX250（IMX250 全局快门 5MP） | 标准 UVC 免驱 + 6-pin 触发口，OpenCV 直开 |
| 国产 UVC 工业相机 | 迈德威视 / 华睿 UVC 系列 | 下单前确认：① 标准 UVC 免驱 ② 带触发线 |

触发方式：主机 GPIO / 信号发生器发同步脉冲 → 多路帧级同步（微秒级）。

### 2.4 USB 控制器方案（4 路关键）

**原则：每路摄像头独占一个 USB3 控制器；绝不用 USB Hub（共享带宽 + isochronous 调度问题必掉帧）。**

| 方案 | 做法 | 成本 | 备注 |
|---|---|---|---|
| 推荐 | 2× StarTech PEXUSB314A2V（2 端口 × 2 独立控制器） | ~¥500-700/张 | 4 路全独立控制器 |
| 省钱起步 | 主板原生 USB3 口（一般 2 个控制器）先测 4×720p30 MJPEG（~120 MB/s，单控制器 ~400 MB/s 富余） | ¥0 | 不够再加卡 |
| 避坑 | 单控制器 4 口卡、USB Hub | — | ❌ 必掉帧 |

### 2.5 清晰度保障清单

1. 全局快门 > 卷帘快门（第一要素）
2. 传感器 ≥ 1/3"（OV9281 为 1/4"，够用但别更小）
3. 分辨率 720p 起（行为学分析足够；2MP 更稳）
4. f1.4 大光圈定焦，不用广角
5. 均匀光照（白光或红外+无 IR-cut），直接决定参考帧比对和后续 DLC 分析质量

---

## 3. 同步架构（核心设计）

### 3.1 总体信号流

```
┌──────────────────────── VideoRec 主机 ────────────────────────┐
│                                                               │
│  ┌─ USB3 控制器1 ─ Cam0 ─┐                                    │
│  ├─ USB3 控制器2 ─ Cam1  ├─ 4 路视频 (MJPEG 30fps)            │
│  ├─ USB3 控制器3 ─ Cam2  │                                    │
│  └─ USB3 控制器4 ─ Cam3 ─┘                                    │
│                                                               │
│  ┌─ USB 串口 ─ TTL 桥（STM32/Arduino，自带微秒时钟） ────────┐ │
│  │   ├─ TTL out A: 30Hz 帧同步脉冲 ────────► 光纤 TTL in     │ │
│  │   ├─ TTL out B: 录制开始/停止脉冲 ──────► 光遗传 TTL in    │ │
│  │   └─ TTL in:  设备事件脉冲 ◄───────────── 光纤/光遗传 TTL out│ │
│  └───────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘

光纤侧：帧同步脉冲自动写入光纤时间轴（设备自带功能）
光遗传侧：刺激起止 = TTL out 事件，由视频侧记录帧号
事后对齐：光纤导出时间戳 ↔ TTL 事件表 ↔ 视频帧号 三表映射
```

### 3.2 TTL 桥硬件选择

| 方案 | 硬件 | 成本 | 精度 | 说明 |
|---|---|---|---|---|
| **推荐** | STM32/Arduino + USB 串口 | ¥30-80 | μs 级 | **时间戳在单片机端打**（串口上报），PC 端轮询延迟不可靠 |
| 省事 | LabJack U3-LV / NI USB-6501 数字 IO | ¥800-1200 | μs 级 | 专业 DAQ，SDK 成熟，无固件开发 |
| 纯软件 | 声卡/并口 hack | — | ❌ | 不推荐 |

要点：**脉冲时间戳必须由 TTL 桥自己打**（单片机晶振计时），PC 通过串口异步接收，二者时钟在录制开始时用"开始脉冲"对齐。

### 3.3 三种同步信号的定义

| 信号 | 方向 | 频率/形态 | 用途 |
|---|---|---|---|
| 帧同步脉冲 | VideoRec → 光纤 TTL in | 30Hz 方波，脉宽 5-10ms | 每帧一个脉冲 → 光纤时间轴标记视频帧号 |
| 录制开始/停止 | VideoRec → 光遗传 TTL in | 单脉冲（可选） | 触发外部设备同步开始/结束 |
| 设备事件 | 光纤/光遗传 TTL out → VideoRec | 事件脉冲 | 刺激起止、采集标记 → 记录帧号 |

注：光遗传 TTL in 通常用于触发刺激而非帧同步，故帧同步只接光纤；光遗传用"开始/停止"对齐 + 事件记录。

### 3.4 时间戳对齐算法（事后）

```
输入：
  A = VideoRec 帧时间戳表  (frame_idx, monotonic_s, wall_s)
  B = TTL 事件表           (event_idx, ttl_bridge_s, 类型: 帧脉冲/事件)
  C = 光纤导出时间轴        (fiber_t, 采样值…)
步骤：
  1. 用"录制开始脉冲"对齐 VideoRec 与 TTL 桥时钟（偏移量 + 线性漂移校正）
  2. 帧脉冲序列 B 与帧时间戳 A 一一对应（数量相等，顺序一致）→ 帧号 ↔ TTL 桥时间
  3. 光纤时间轴 C 中每个 TTL 脉冲时间戳 ↔ 帧号 → 插值得到任意光纤采样点对应的视频帧
输出：
  sync_map.csv: (fiber_t, frame_idx, offset_ms)
  对齐报告：最大/平均偏差、缺帧数、脉冲丢失报警
```

### 3.5 无 TTL 桥的降级方案

光纤软件若支持"外部 TTL 触发开始采集"：VideoRec 输出开始脉冲 → 光纤自动开录 → 各自时间戳 + 事件表事后对齐（秒级事件够用，无需额外硬件）。

---

## 4. VideoRec 软件改造计划（里程碑）

### M1 — 多摄像头模式
- `core/camera_manager.py`：Camera 多实例管理（枚举、独立参数、独立 Recorder）
- UI：预览网格（1/2/4 分屏，点击放大单路）、每路状态灯（连接/录制中/丢帧）
- 同步启停：一个"开始/停止"按钮控制全部；每路独立 timestamps.csv / notes.csv / meta.json
- 触发联动：触发区点击 → 全部启动（或按配置只启动指定路）
- 参考帧/虚影：每路独立 ReferenceManager（各自项目子目录）

### M2 — TTL 同步桥
- `core/sync_bridge.py`：串口协议封装（命令：开始/停止/输出帧脉冲/上报事件）
- 帧同步脉冲输出：每写一帧 → 串口命令 → TTL 桥输出 5ms 脉冲（30Hz）
- 事件输入记录：TTL 桥主动上报（含桥端时间戳）→ 写入 `sync_events.csv`
- 录制开始：输出 start 脉冲 + 记录三端时钟偏移
- GUI：同步面板（设备连接状态、脉冲计数、丢失脉冲率、时钟偏移显示）

### M3 — 对齐工具
- `tools/sync_align.py`：导入光纤/光遗传导出时间戳 → 执行 §3.4 算法 → 生成 `sync_map.csv` + 对齐报告（偏差、丢帧、脉冲丢失）
- GUI 集成：录制完成后一键对齐，可视化偏差曲线

### 排期估计
| 里程碑 | 工作量 | 依赖 |
|---|---|---|
| M1 | ~0.5 天 | 硬件（4 相机 + USB 卡） |
| M2 | ~1 天 | TTL 桥硬件（STM32/Arduino，含固件） |
| M3 | ~0.5 天 | M2 数据格式 |

---

## 5. 验证计划

1. **4 路稳定性**：4×720p30 MJPEG 连续录制 30 分钟，统计每路丢帧率 < 1%
2. **视频↔视频同步**：录制电子秒表/闪光 LED，对比 4 路时间戳偏差（目标 < 1 帧）
3. **TTL 帧同步**：光纤侧接收 30Hz 脉冲，检查脉冲计数 = 视频帧数（±0）
4. **事件对齐**：光遗传输出已知时间序列刺激，验证 sync_map.csv 帧号误差 < 1 帧
5. **长时漂移**：1 小时录制，检查时钟漂移校正后最大偏差 < 5ms

---

## 6. 风险与备选

| 风险 | 影响 | 缓解 |
|---|---|---|
| 杂牌 UVC 相机固件差异大（曝光/白平衡/帧率不稳定） | 画质/帧率异常 | 优先知名传感器（OV9281/OV2311）；到货先做稳定性测试 |
| 单 USB3 控制器 4 路带宽不足 | 掉帧 | 2× 双控制器卡兜底 |
| 光纤设备 TTL in 频率上限 < 30Hz | 帧同步失败 | 降帧率到 25Hz 或改"每 N 帧一脉冲 + 插值" |
| 光纤/光遗传软件时间戳格式不开放 | 无法自动对齐 | 导出 CSV 手工映射；或降级为事件级同步 |
| 串口桥时钟漂移 | 长时间偏差 | 每分钟自动发送校时脉冲，事后线性漂移校正 |

---

## 7. 待确认事项

- [ ] 用户现有摄像头型号/接口（评估是否可复用）
- [ ] fiberphotometry 品牌型号（Doric / RWD / Neurophotometrics…）→ 确认 TTL in 频率上限与时间戳导出格式
- [ ] 光遗传设备 TTL 接口（触发 in 还是输出刺激时间 out）
- [ ] 是否需要帧级同步（方案 B 触发相机）还是软件同步起步（方案 A）
- [ ] 采购预算与审批
